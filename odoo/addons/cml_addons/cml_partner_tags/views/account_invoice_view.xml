<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="invoice_supplier_form_tags" model="ir.ui.view">
        <field name="name">account.invoice.supplier.form.tags</field>
        <field name="model">account.invoice</field>
        <field name="inherit_id" ref="account.invoice_supplier_form"/>
        <field name="arch" type="xml">
            <field name="partner_id" position="after">
                <field name="partner_category_id" widget="many2many_tags" options="{'color_field': 'color', 'no_create_edit': True}"/>
            </field>
        </field>
    </record>

    <record id="invoice_form_tags" model="ir.ui.view">
        <field name="name">account.invoice.form.tags</field>
        <field name="model">account.invoice</field>
        <field name="inherit_id" ref="account.invoice_form"/>
        <field name="arch" type="xml">
            <field name="partner_id" position="after">
                <field name="partner_category_id" widget="many2many_tags" options="{'color_field': 'color', 'no_create_edit': True}"/>
            </field>
        </field>
    </record>

    <record id="view_account_invoice_filter_tags" model="ir.ui.view">
        <field name="name">account.invoice.select.tags</field>
        <field name="model">account.invoice</field>
        <field name="inherit_id" ref="account.view_account_invoice_filter"/>
        <field name="arch" type="xml">
            <field name="number" position="after">
                <field name="partner_category_id" string="Partner Tags" filter_domain="[('partner_category_id', 'ilike', self)]"/>
            </field>
        </field>
    </record>

</odoo>
